# SleepTimer
Simple sleep timer GUI for windows 

# Environment Setup
Run the script
```ps1
.\env_setup.bat
```

# Packaging into .exe
Run the script
```ps1
.\package.bat
```

The packaged .exe can be found under `dist` directory
